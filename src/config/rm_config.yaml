model:
  name: "distilroberta-base"
  output_dir: "src/models/sft_model"

data:
  path: "src/data/commongen_rm_dataset.jsonl"
  test_size: 0.1
  max_length: 128

training:
  batch_size: 16
  epochs: 1
  lr: 2e-5
  eval_steps: 50